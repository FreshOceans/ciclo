<!DOCTYPE html>
<html>
  <head>
    <title>Ciclo</title>
    <%= csrf_meta_tags %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= include_gon %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=" + GOOGLE_JS_KEY %>
    <!-- <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Amaranth|Raleway|Croissant+One|Shrikhand" rel="stylesheet"> -->
  </head>
  <body>

    <div class="top-container">
        <div class="row">
            <div id="menu_container" class="col-12">
                <%#= render :partial => "/users/menu" %>
                <%= render :partial => "/users/navbar" %>
            </div>
        </div>
        <div class="row">
            <div id="yield-container" class="col-12">
                <%= yield %>
            </div>
        </div>
        <div class="row">
            <div id="footer" class="col-12">
            <% if notice %>
                <p id="notice"> <%= notice %> </p>
            <% end %>
            <% if alert %>
                <p id ="alert"> <%= alert %> </p>
            <% end %>
            </div>
        </div>
    </div>
  </body>
</html>
